// ✅ NEW: Enum to match the backend SessionType
export enum SessionType {
    PHYSICAL = 'PHYSICAL',
    ONLINE = 'ONLINE',
}

export enum SalesStartRuleType {
    IMMEDIATE = 'IMMEDIATE',
    ROLLING = 'ROLLING',
    FIXED = 'FIXED',
}